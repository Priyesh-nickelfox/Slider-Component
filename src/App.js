import { Box } from '@mui/material';
import  SliderCarousel from './Slider/ SliderCarousel';

function App() {
  return (
    <Box sx={{padding: '8%', backgroundColor: 'grey', height: '100vh'}}>
      <SliderCarousel />
    </Box>
  );
}

export default App;